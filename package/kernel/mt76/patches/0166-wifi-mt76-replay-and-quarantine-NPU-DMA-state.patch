From d1c1f31b9e7fe9f5f744500d4698d025a196bc47 Mon Sep 17 00:00:00 2001
From: Yanghan Ye <yyh94306@gmail.com>
Date: Sat, 29 Aug 2026 22:13:03 +0800
Subject: [PATCH] wifi: mt76: replay and quarantine NPU DMA state

A mailbox health check does not reboot the NPU. If it fails, force a
core recovery and retry the failed command once. A runtime RRO release
failure then requests a full Wi-Fi reset with an explicit NPU reason.

Even after forced core recovery, complete pause, drain, and detach before
mutating consumer DMA rings. Core recovery preserves firmware state but
is not the consumer DMA-release fence.

Replay reserved memory, every RRO address, and all MT7996 hardware
configuration after recovery. Publish RRO addresses only from this full
initialization path, both at probe and after reset.

If the terminal hard-stop fence also fails, remove every consumer DMA
devres action from a tracked allocation list and intentionally retain
the DMA memory and supplier references. Stop work, WED, interrupts, and
WFDMA without freeing page pools, RRO memory, tokens, or the mt76 object.
Keep the HIF2 reference until a successful fence.

Signed-off-by: Yanghan Ye <yyh94306@gmail.com>
---
 dma.c           |  28 ++++++++++++
 mac80211.c      |   2 +
 mt76.h          |  24 +++++++++++
 mt7996/init.c   |  84 ++++++++++++++++++++++++++----------
 mt7996/mac.c    |  15 +++++--
 mt7996/mt7996.h |  13 +++++-
 mt7996/npu.c    |  49 ++++++++++++++++++---
 mt7996/pci.c    |  21 ++++++---
 npu.c           | 111 ++++++++++++++++++++++++++++++++----------------
 9 files changed, 273 insertions(+), 74 deletions(-)

diff --git a/dma.c b/dma.c
index db8e4be..fbfabee 100644
--- a/dma.c
+++ b/dma.c
@@ -806,6 +806,7 @@ int mt76_dma_rx_fill(struct mt76_dev *dev, struct mt76_queue *q,
 }
 
 struct mt76_dma_allocation {
+	struct list_head list;
 	struct device *dma_dev;
 	void *vaddr;
 	dma_addr_t dma_addr;
@@ -845,8 +846,18 @@ void *mt76_dma_alloc_coherent(struct mt76_dev *dev, size_t size,
 		return NULL;
 	}
 
+	mutex_lock(&dev->dma_alloc_mutex);
+	if (dev->dma_alloc_quarantined) {
+		mutex_unlock(&dev->dma_alloc_mutex);
+		mt76_dma_free_coherent(allocation);
+		return NULL;
+	}
+
 	err = devm_add_action_or_reset(dev->dev, mt76_dma_free_coherent,
 				       allocation);
+	if (!err)
+		list_add_tail(&allocation->list, &dev->dma_allocations);
+	mutex_unlock(&dev->dma_alloc_mutex);
 	if (err)
 		return NULL;
 
@@ -856,6 +867,23 @@ void *mt76_dma_alloc_coherent(struct mt76_dev *dev, size_t size,
 }
 EXPORT_SYMBOL_GPL(mt76_dma_alloc_coherent);
 
+void mt76_dma_quarantine_allocations(struct mt76_dev *dev)
+{
+	struct mt76_dma_allocation *allocation, *tmp;
+	LIST_HEAD(list);
+
+	mutex_lock(&dev->dma_alloc_mutex);
+	dev->dma_alloc_quarantined = true;
+	list_splice_init(&dev->dma_allocations, &list);
+	mutex_unlock(&dev->dma_alloc_mutex);
+
+	list_for_each_entry_safe(allocation, tmp, &list, list) {
+		devm_remove_action(dev->dev, mt76_dma_free_coherent, allocation);
+		list_del_init(&allocation->list);
+	}
+}
+EXPORT_SYMBOL_GPL(mt76_dma_quarantine_allocations);
+
 static int
 mt76_dma_alloc_queue(struct mt76_dev *dev, struct mt76_queue *q,
 		     int idx, int n_desc, int bufsize,
diff --git a/mac80211.c b/mac80211.c
index 457194a..f67f215 100644
--- a/mac80211.c
+++ b/mac80211.c
@@ -696,6 +696,8 @@ mt76_alloc_device(struct device *pdev, unsigned int size,
 	spin_lock_init(&dev->status_lock);
 	spin_lock_init(&dev->wed_lock);
 	mutex_init(&dev->mutex);
+	mutex_init(&dev->dma_alloc_mutex);
+	INIT_LIST_HEAD(&dev->dma_allocations);
 	init_waitqueue_head(&dev->tx_wait);
 
 	skb_queue_head_init(&dev->mcu.res_q);
diff --git a/mt76.h b/mt76.h
index 205baf4..d98ac50 100644
--- a/mt76.h
+++ b/mt76.h
@@ -952,6 +952,10 @@ struct mt76_dev {
 	u32 rx_ampdu_ref;
 
 	struct mutex mutex;
+	/* protects NPU/WED DMA allocation lifetime state */
+	struct mutex dma_alloc_mutex;
+	struct list_head dma_allocations;
+	bool dma_alloc_quarantined;
 
 	const struct mt76_bus_ops *bus;
 	const struct mt76_driver_ops *drv;
@@ -1333,6 +1337,7 @@ void mt76_unregister_device(struct mt76_dev *dev);
 void mt76_free_device(struct mt76_dev *dev);
 void *mt76_dma_alloc_coherent(struct mt76_dev *dev, size_t size,
 			      dma_addr_t *dma_handle, gfp_t gfp);
+void mt76_dma_quarantine_allocations(struct mt76_dev *dev);
 void mt76_reset_device(struct mt76_dev *dev);
 void mt76_unregister_phy(struct mt76_phy *phy);
 
@@ -1681,6 +1686,8 @@ int mt76_npu_fill_rx_queue(struct mt76_dev *dev, struct mt76_queue *q);
 void mt76_npu_queue_cleanup(struct mt76_dev *dev, struct mt76_queue *q);
 void mt76_npu_disable_irqs(struct mt76_dev *dev);
 int mt76_npu_init(struct mt76_dev *dev, phys_addr_t phy_addr, int type);
+int mt76_npu_init_reserved_memory(struct mt76_dev *dev,
+				  struct airoha_npu *npu);
 void mt76_npu_deinit(struct mt76_dev *dev);
 void mt76_npu_queue_setup(struct mt76_dev *dev, struct mt76_queue *q);
 void mt76_npu_txdesc_cleanup(struct mt76_queue *q, int index);
@@ -1690,6 +1697,9 @@ int mt76_npu_net_setup_tc(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
 			    u32 direction, u32 i_count_addr,
 			    u32 o_status_addr, u32 o_count_addr);
+int __mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+			      u32 direction, u32 i_count_addr,
+			      u32 o_status_addr, u32 o_count_addr);
 #else
 static inline void mt76_npu_check_ppe(struct mt76_dev *dev,
 				      struct sk_buff *skb, u32 info)
@@ -1735,6 +1745,12 @@ static inline int mt76_npu_init(struct mt76_dev *dev, phys_addr_t phy_addr,
 	return 0;
 }
 
+static inline int
+mt76_npu_init_reserved_memory(struct mt76_dev *dev, struct airoha_npu *npu)
+{
+	return -EOPNOTSUPP;
+}
+
 static inline void mt76_npu_deinit(struct mt76_dev *dev)
 {
 }
@@ -1764,6 +1780,14 @@ static inline int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
 {
 	return -EOPNOTSUPP;
 }
+
+static inline int __mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+					    u32 direction, u32 i_count_addr,
+					    u32 o_status_addr,
+					    u32 o_count_addr)
+{
+	return -EOPNOTSUPP;
+}
 #endif /* CONFIG_MT76_NPU */
 
 static inline bool mt76_npu_device_active(struct mt76_dev *dev)
diff --git a/mt7996/init.c b/mt7996/init.c
index 67e4602..8aa2fb3 100644
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -976,12 +976,35 @@ void mt7996_rro_hw_init(struct mt7996_dev *dev)
 		MT_RRO_HOST_INT_ENA_HOST_RRO_DONE_ENA);
 }
 
+int __mt7996_npu_rro_addr_init(struct mt7996_dev *dev)
+{
+	int i, err;
+
+	lockdep_assert_held(&dev->mt76.mutex);
+
+	if (!is_mt7996(&dev->mt76) ||
+	    !mt76_npu_device_active(&dev->mt76))
+		return 0;
+
+	for (i = 0; i < ARRAY_SIZE(dev->wed_rro.addr_elem); i++) {
+		err = __mt76_npu_send_txrx_addr(&dev->mt76, 0, i,
+						dev->wed_rro.addr_elem[i].phy_addr,
+						0, 0);
+		if (err)
+			return err;
+	}
+
+	return __mt76_npu_send_txrx_addr(&dev->mt76, 1, 0,
+						 dev->wed_rro.session.phy_addr,
+						 0, 0);
+}
+
 static int mt7996_wed_rro_init(struct mt7996_dev *dev)
 {
 	u32 val = FIELD_PREP(WED_RRO_ADDR_SIGNATURE_MASK, 0xff);
 	struct mt7996_wed_rro_addr *addr;
 	void *ptr;
-	int i, err;
+	int i;
 
 	if (!mt7996_has_hwrro(dev))
 		return 0;
@@ -1019,15 +1042,6 @@ static int mt7996_wed_rro_init(struct mt7996_dev *dev)
 			addr++;
 		}
 
-		if (is_mt7996(&dev->mt76) &&
-		    mt76_npu_device_active(&dev->mt76)) {
-			err = mt76_npu_send_txrx_addr(&dev->mt76, 0, i,
-						      dev->wed_rro.addr_elem[i].phy_addr,
-						      0, 0);
-			if (err)
-				return err;
-		}
-
 #ifdef CONFIG_NET_MEDIATEK_SOC_WED
 		if (mtk_wed_device_active(&dev->mt76.mmio.wed) &&
 		    mtk_wed_get_rx_capa(&dev->mt76.mmio.wed)) {
@@ -1088,14 +1102,6 @@ static int mt7996_wed_rro_init(struct mt7996_dev *dev)
 		addr++;
 	}
 
-	if (is_mt7996(&dev->mt76) && mt76_npu_device_active(&dev->mt76)) {
-		err = mt76_npu_send_txrx_addr(&dev->mt76, 1, 0,
-					      dev->wed_rro.session.phy_addr,
-					      0, 0);
-		if (err)
-			return err;
-	}
-
 	mt7996_rro_hw_init(dev);
 
 	return mt7996_dma_rro_init(dev);
@@ -1198,6 +1204,9 @@ static void mt7996_wed_rro_work(struct work_struct *work)
 					/* The NPU may still reference this session. Do not
 					 * tell the WLAN firmware that it can be reused.
 					 */
+					dev->recovery.npu_reset = true;
+					dev->recovery.restart = true;
+					queue_work(dev->mt76.wq, &dev->reset_work);
 					goto out;
 				}
 			}
@@ -1874,7 +1883,30 @@ error:
 	return ret;
 }
 
-void mt7996_unregister_device(struct mt7996_dev *dev)
+void mt7996_quarantine_device(struct mt7996_dev *dev)
+{
+	dev->recovery.hw_full_reset = true;
+	cancel_work_sync(&dev->dump_work);
+	cancel_work_sync(&dev->reset_work);
+	dev->recovery.hw_full_reset = true;
+	mt7996_wed_rro_work_stop(dev);
+	cancel_work_sync(&dev->rc_work);
+
+	if (test_bit(MT76_STATE_REGISTERED, &dev->mphy.state))
+		ieee80211_stop_queues(mt76_hw(dev));
+	mt76_worker_disable(&dev->mt76.tx_worker);
+
+	if (mtk_wed_device_active(&dev->mt76.mmio.wed_hif2))
+		mtk_wed_device_stop(&dev->mt76.mmio.wed_hif2);
+	if (mtk_wed_device_active(&dev->mt76.mmio.wed))
+		mtk_wed_device_stop(&dev->mt76.mmio.wed);
+
+	mt76_npu_disable_irqs(&dev->mt76);
+	mt7996_irq_disable(dev, ~0);
+	mt7996_dma_disable(dev, true);
+}
+
+int mt7996_unregister_device(struct mt7996_dev *dev)
 {
 	int err;
 
@@ -1889,7 +1921,8 @@ void mt7996_unregister_device(struct mt7996_dev *dev)
 		dev_crit(dev->mt76.dev,
 			 "failed to fence NPU DMA; refusing to release resources: %d\n",
 			 err);
-		return;
+		mt7996_quarantine_device(dev);
+		return err;
 	}
 	mt7996_unregister_phy(mt7996_phy3(dev));
 	mt7996_unregister_phy(mt7996_phy2(dev));
@@ -1907,9 +1940,11 @@ void mt7996_unregister_device(struct mt7996_dev *dev)
 	tasklet_disable(&dev->mt76.irq_tasklet);
 
 	mt76_free_device(&dev->mt76);
+
+	return 0;
 }
 
-void mt7996_shutdown_device(struct mt7996_dev *dev)
+int mt7996_shutdown_device(struct mt7996_dev *dev)
 {
 	int err;
 
@@ -1925,9 +1960,12 @@ void mt7996_shutdown_device(struct mt7996_dev *dev)
 		mtk_wed_device_stop(&dev->mt76.mmio.wed);
 
 	err = mt7996_npu_hw_stop_teardown(dev);
-	if (err)
+	if (err) {
 		dev_emerg(dev->mt76.dev,
 			  "failed to fence NPU during shutdown: %d\n", err);
+		mt7996_quarantine_device(dev);
+		return err;
+	}
 
 	mt76_npu_disable_irqs(&dev->mt76);
 	mt7996_irq_disable(dev, ~0);
@@ -1937,4 +1975,6 @@ void mt7996_shutdown_device(struct mt7996_dev *dev)
 		dev->recovery.hw_full_reset = true;
 		mt7996_wfsys_reset(dev);
 	}
+
+	return 0;
 }
diff --git a/mt7996/mac.c b/mt7996/mac.c
index 11e0e9b..e0f50e0 100644
--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -2340,7 +2340,7 @@ static int mt7996_mac_full_reset(struct mt7996_dev *dev)
 	struct ieee80211_hw *hw = mt76_hw(dev);
 	struct mt7996_phy *phy;
 	LIST_HEAD(list);
-	int i, err;
+	int i, err = 0;
 
 	dev->recovery.hw_full_reset = true;
 
@@ -2354,10 +2354,13 @@ static int mt7996_mac_full_reset(struct mt7996_dev *dev)
 	if (mtk_wed_device_active(&dev->mt76.mmio.wed))
 		mtk_wed_device_stop(&dev->mt76.mmio.wed);
 
-	err = mt7996_npu_hw_stop(dev);
+	if (dev->recovery.npu_reset)
+		err = mt7996_npu_hw_recover(dev);
+	if (!err)
+		err = mt7996_npu_hw_stop(dev);
 	if (err) {
 		mt7996_npu_recovery_failed(dev,
-					   "NPU stop before full reset", err);
+					   "NPU quiesce before full reset", err);
 		return err;
 	}
 
@@ -2445,7 +2448,10 @@ void mt7996_mac_reset_work(struct work_struct *work)
 		mt76_clear(dev, MT_WFDMA0_MCU_HOST_INT_ENA,
 			   MT_MCU_CMD_WDT_MASK);
 
-		if (READ_ONCE(dev->recovery.state) & MT_MCU_CMD_WA_WDT)
+		if (dev->recovery.npu_reset)
+			dev_warn(dev->mt76.dev,
+				 "resetting Wi-Fi after an NPU mailbox failure\n");
+		else if (READ_ONCE(dev->recovery.state) & MT_MCU_CMD_WA_WDT)
 			dev->recovery.wa_reset_count++;
 		else
 			dev->recovery.wm_reset_count++;
@@ -2462,6 +2468,7 @@ void mt7996_mac_reset_work(struct work_struct *work)
 
 		dev->recovery.state = MT_MCU_CMD_NORMAL_STATE;
 		dev->recovery.restart = false;
+		dev->recovery.npu_reset = false;
 		return;
 	}
 
diff --git a/mt7996/mt7996.h b/mt7996/mt7996.h
index 57b6b4f..ad4b4fa 100644
--- a/mt7996/mt7996.h
+++ b/mt7996/mt7996.h
@@ -500,6 +500,7 @@ struct mt7996_dev {
 		bool hw_full_reset:1;
 		bool hw_init_done:1;
 		bool restart:1;
+		bool npu_reset:1;
 	} recovery;
 
 	/* protects coredump data */
@@ -735,10 +736,12 @@ void mt7996_wfsys_reset(struct mt7996_dev *dev);
 irqreturn_t mt7996_irq_handler(int irq, void *dev_instance);
 u64 __mt7996_get_tsf(struct ieee80211_hw *hw, struct mt7996_vif_link *link);
 int mt7996_register_device(struct mt7996_dev *dev);
-void mt7996_unregister_device(struct mt7996_dev *dev);
-void mt7996_shutdown_device(struct mt7996_dev *dev);
+int mt7996_unregister_device(struct mt7996_dev *dev);
+int mt7996_shutdown_device(struct mt7996_dev *dev);
+void mt7996_quarantine_device(struct mt7996_dev *dev);
 void mt7996_wed_rro_work_stop(struct mt7996_dev *dev);
 void mt7996_wed_rro_work_start(struct mt7996_dev *dev);
+int __mt7996_npu_rro_addr_init(struct mt7996_dev *dev);
 int mt7996_vif_link_add(struct mt76_phy *mphy, struct ieee80211_vif *vif,
 			struct ieee80211_bss_conf *link_conf,
 			struct mt76_vif_link *mlink);
@@ -1027,6 +1030,7 @@ void mt7996_dma_rro_start(struct mt7996_dev *dev);
 #ifdef CONFIG_MT7996_NPU
 int __mt7996_npu_hw_init(struct mt7996_dev *dev);
 int mt7996_npu_hw_init(struct mt7996_dev *dev);
+int mt7996_npu_hw_recover(struct mt7996_dev *dev);
 int mt7996_npu_hw_stop(struct mt7996_dev *dev);
 int mt7996_npu_hw_stop_teardown(struct mt7996_dev *dev);
 int mt7996_npu_rx_queues_init(struct mt7996_dev *dev);
@@ -1041,6 +1045,11 @@ static inline int mt7996_npu_hw_init(struct mt7996_dev *dev)
 	return 0;
 }
 
+static inline int mt7996_npu_hw_recover(struct mt7996_dev *dev)
+{
+	return 0;
+}
+
 static inline int mt7996_npu_hw_stop(struct mt7996_dev *dev)
 {
 	return 0;
diff --git a/mt7996/npu.c b/mt7996/npu.c
index 1e5c792..f493409 100644
--- a/mt7996/npu.c
+++ b/mt7996/npu.c
@@ -552,6 +552,22 @@ static int mt7996_npu_hw_init_once(struct mt7996_dev *dev,
 	return 0;
 }
 
+static int mt7996_npu_full_init_once(struct mt7996_dev *dev,
+				     struct airoha_npu *npu)
+{
+	int err;
+
+	err = mt76_npu_init_reserved_memory(&dev->mt76, npu);
+	if (err)
+		return err;
+
+	err = __mt7996_npu_rro_addr_init(dev);
+	if (err)
+		return err;
+
+	return mt7996_npu_hw_init_once(dev, npu);
+}
+
 static int mt7996_npu_prepare_retry(struct mt7996_dev *dev,
 				    struct airoha_npu *npu, int err,
 				    const char *operation)
@@ -572,9 +588,15 @@ static int mt7996_npu_prepare_retry(struct mt7996_dev *dev,
 	dev_warn(dev->mt76.dev, "%s timed out, recovering mailbox\n",
 		 operation);
 	err = airoha_npu_recover_mailbox(npu);
-	if (err)
-		dev_err(dev->mt76.dev, "failed to recover NPU mailbox: %d\n",
-			err);
+	if (err) {
+		dev_warn(dev->mt76.dev,
+			 "NPU mailbox health check failed: %d; forcing recovery\n",
+			 err);
+		err = airoha_npu_recover(npu);
+		if (err)
+			dev_err(dev->mt76.dev,
+				"failed to recover NPU core 0: %d\n", err);
+	}
 
 	return err;
 }
@@ -588,7 +610,7 @@ int __mt7996_npu_hw_init(struct mt7996_dev *dev)
 	if (!npu)
 		return 0;
 
-	err = mt7996_npu_hw_init_once(dev, npu);
+	err = mt7996_npu_full_init_once(dev, npu);
 	if (err != -ETIMEDOUT && err != -EAGAIN)
 		return err;
 
@@ -597,7 +619,7 @@ int __mt7996_npu_hw_init(struct mt7996_dev *dev)
 	if (err)
 		return err;
 
-	err = mt7996_npu_hw_init_once(dev, npu);
+	err = mt7996_npu_full_init_once(dev, npu);
 	if (err)
 		dev_err(dev->mt76.dev,
 			"NPU hardware initialization retry failed: %d\n", err);
@@ -640,6 +662,21 @@ int mt7996_npu_hw_init(struct mt7996_dev *dev)
 	return err;
 }
 
+int mt7996_npu_hw_recover(struct mt7996_dev *dev)
+{
+	struct airoha_npu *npu;
+	int err = 0;
+
+	mutex_lock(&dev->mt76.mutex);
+	npu = rcu_dereference_protected(dev->mt76.mmio.npu,
+					lockdep_is_held(&dev->mt76.mutex));
+	if (npu)
+		err = airoha_npu_recover(npu);
+	mutex_unlock(&dev->mt76.mutex);
+
+	return err;
+}
+
 static int mt7996_npu_hw_stop_once(struct mt7996_dev *dev,
 				   struct airoha_npu *npu,
 				   bool *drain_timeout)
@@ -743,6 +780,8 @@ int mt7996_npu_hw_stop_teardown(struct mt7996_dev *dev)
 	if (err)
 		dev_err(dev->mt76.dev,
 			"failed to fence NPU DMA with hard stop: %d\n", err);
+	if (err)
+		mt76_dma_quarantine_allocations(&dev->mt76);
 
 	return err;
 }
diff --git a/mt7996/pci.c b/mt7996/pci.c
index 456c65c..fa412b8 100644
--- a/mt7996/pci.c
+++ b/mt7996/pci.c
@@ -200,10 +200,13 @@ static int mt7996_pci_probe(struct pci_dev *pdev,
 
 free_hif2_irq:
 	npu_ret = mt7996_npu_hw_stop_teardown(dev);
-	if (npu_ret)
+	if (npu_ret) {
 		dev_crit(mdev->dev,
 			 "failed to fence NPU DMA during probe unwind: %d\n",
 			 npu_ret);
+		mt7996_quarantine_device(dev);
+		return npu_ret;
+	}
 	mt76_npu_rx_queues_deinit(mdev);
 	if (dev->hif2)
 		devm_free_irq(mdev->dev, hif2_irq, dev);
@@ -243,13 +246,17 @@ static void mt7996_hif_shutdown(struct pci_dev *pdev)
 
 static void mt7996_pci_remove(struct pci_dev *pdev)
 {
+	struct mt7996_hif *hif2;
 	struct mt76_dev *mdev;
 	struct mt7996_dev *dev;
+	int err;
 
 	mdev = pci_get_drvdata(pdev);
 	dev = container_of(mdev, struct mt7996_dev, mt76);
-	mt7996_put_hif2(dev->hif2);
-	mt7996_unregister_device(dev);
+	hif2 = dev->hif2;
+	err = mt7996_unregister_device(dev);
+	if (!err)
+		mt7996_put_hif2(hif2);
 }
 
 static void mt7996_pci_shutdown(struct pci_dev *pdev)
@@ -257,13 +264,17 @@ static void mt7996_pci_shutdown(struct pci_dev *pdev)
 	struct mt76_dev *mdev = pci_get_drvdata(pdev);
 	struct mt7996_dev *dev;
 	struct pci_dev *hif2_dev = NULL;
+	struct mt7996_hif *hif2;
+	int err;
 
 	dev = container_of(mdev, struct mt7996_dev, mt76);
+	hif2 = dev->hif2;
 	if (dev->hif2)
 		hif2_dev = pci_dev_get(to_pci_dev(dev->hif2->dev));
 
-	mt7996_put_hif2(dev->hif2);
-	mt7996_shutdown_device(dev);
+	err = mt7996_shutdown_device(dev);
+	if (!err)
+		mt7996_put_hif2(hif2);
 	pci_clear_master(pdev);
 
 	if (hif2_dev) {
diff --git a/npu.c b/npu.c
index 7ece6fd..0a04816 100644
--- a/npu.c
+++ b/npu.c
@@ -497,9 +497,9 @@ int mt76_npu_net_setup_tc(struct ieee80211_hw *hw, struct ieee80211_vif *vif,
 }
 EXPORT_SYMBOL_GPL(mt76_npu_net_setup_tc);
 
-int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
-			    u32 direction, u32 i_count_addr,
-			    u32 o_status_addr, u32 o_count_addr)
+int __mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+			      u32 direction, u32 i_count_addr,
+			      u32 o_status_addr, u32 o_count_addr)
 {
 	struct {
 		__le32 dir;
@@ -516,11 +516,11 @@ int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
 	bool retry;
 	int err = -ENODEV;
 
-	mutex_lock(&dev->mutex);
+	lockdep_assert_held(&dev->mutex);
 	npu = rcu_dereference_protected(dev->mmio.npu,
 					lockdep_is_held(&dev->mutex));
 	if (!npu)
-		goto unlock;
+		return err;
 
 	err = airoha_npu_wlan_send_msg(npu, ifindex,
 				       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR,
@@ -528,14 +528,34 @@ int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
 	retry = err == -ETIMEDOUT || err == -EAGAIN;
 	if (err == -EAGAIN)
 		err = airoha_npu_wait_recovery(npu);
-	else if (err == -ETIMEDOUT)
+	else if (err == -ETIMEDOUT) {
 		err = airoha_npu_recover_mailbox(npu);
+		if (err) {
+			dev_warn(dev->dev,
+				 "NPU mailbox health check failed: %d; forcing recovery\n",
+				 err);
+			err = airoha_npu_recover(npu);
+		}
+	}
 
 	if (retry && !err)
 		err = airoha_npu_wlan_send_msg(npu, ifindex,
 					       WLAN_FUNC_SET_WAIT_INODE_TXRX_REG_ADDR,
 					       &info, sizeof(info), GFP_KERNEL);
-unlock:
+
+	return err;
+}
+EXPORT_SYMBOL_GPL(__mt76_npu_send_txrx_addr);
+
+int mt76_npu_send_txrx_addr(struct mt76_dev *dev, int ifindex,
+			    u32 direction, u32 i_count_addr,
+			    u32 o_status_addr, u32 o_count_addr)
+{
+	int err;
+
+	mutex_lock(&dev->mutex);
+	err = __mt76_npu_send_txrx_addr(dev, ifindex, direction, i_count_addr,
+					o_status_addr, o_count_addr);
 	mutex_unlock(&dev->mutex);
 
 	return err;
@@ -566,11 +586,57 @@ unlock:
 }
 EXPORT_SYMBOL_GPL(mt76_npu_disable_irqs);
 
+int mt76_npu_init_reserved_memory(struct mt76_dev *dev,
+				  struct airoha_npu *npu)
+{
+	bool retry;
+	int err;
+
+	lockdep_assert_held(&dev->mutex);
+
+	err = airoha_npu_wlan_init_reserved_memory(npu);
+	retry = err == -ETIMEDOUT || err == -EAGAIN;
+	if (err == -EAGAIN) {
+		dev_warn(dev->dev,
+			 "NPU memory initialization interrupted by recovery, waiting\n");
+		err = airoha_npu_wait_recovery(npu);
+		if (err) {
+			dev_err(dev->dev, "NPU recovery failed: %d\n", err);
+			return err;
+		}
+	} else if (err == -ETIMEDOUT) {
+		dev_warn(dev->dev,
+			 "NPU memory initialization timed out, recovering mailbox\n");
+		err = airoha_npu_recover_mailbox(npu);
+		if (err) {
+			dev_warn(dev->dev,
+				 "NPU mailbox health check failed: %d; forcing recovery\n",
+				 err);
+			err = airoha_npu_recover(npu);
+			if (err) {
+				dev_err(dev->dev,
+					"failed to recover NPU core 0: %d\n", err);
+				return err;
+			}
+		}
+	}
+
+	if (retry) {
+		err = airoha_npu_wlan_init_reserved_memory(npu);
+		if (err)
+			dev_err(dev->dev,
+				"NPU memory initialization retry failed: %d\n",
+				err);
+	}
+
+	return err;
+}
+EXPORT_SYMBOL_GPL(mt76_npu_init_reserved_memory);
+
 int mt76_npu_init(struct mt76_dev *dev, phys_addr_t phy_addr, int type)
 {
 	struct airoha_ppe_dev *ppe_dev;
 	struct airoha_npu *npu;
-	bool retry;
 	int err = 0;
 
 	mutex_lock(&dev->mutex);
@@ -597,34 +663,7 @@ int mt76_npu_init(struct mt76_dev *dev, phys_addr_t phy_addr, int type)
 		goto error_npu_put;
 	}
 
-	err = airoha_npu_wlan_init_reserved_memory(npu);
-	retry = err == -ETIMEDOUT || err == -EAGAIN;
-	if (err == -EAGAIN) {
-		dev_warn(dev->dev,
-			 "NPU memory initialization interrupted by recovery, waiting\n");
-		err = airoha_npu_wait_recovery(npu);
-		if (err) {
-			dev_err(dev->dev, "NPU recovery failed: %d\n", err);
-			goto error_ppe_put;
-		}
-	} else if (err == -ETIMEDOUT) {
-		dev_warn(dev->dev,
-			 "NPU memory initialization timed out, recovering mailbox\n");
-		err = airoha_npu_recover_mailbox(npu);
-		if (err) {
-			dev_err(dev->dev, "failed to recover NPU mailbox: %d\n",
-				err);
-			goto error_ppe_put;
-		}
-	}
-
-	if (retry) {
-		err = airoha_npu_wlan_init_reserved_memory(npu);
-		if (err)
-			dev_err(dev->dev,
-				"NPU memory initialization retry failed: %d\n",
-				err);
-	}
+	err = mt76_npu_init_reserved_memory(dev, npu);
 	if (err)
 		goto error_ppe_put;
 
-- 
2.52.0.windows.1

